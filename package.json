{
  "name": "yarn-plugin-dotenv",
  "version": "1.0.1",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jeysal/yarn-plugin-dotenv"
  },
  "main": "index.ts",
  "files": [
    "index.ts",
    "bundles"
  ],
  "scripts": {
    "build": "builder build plugin && yarn plugin import ./bundles/@yarnpkg/plugin-dotenv.js",
    "test": "echo run ./test.bash instead && false"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "find-config": "^1.0.0"
  },
  "devDependencies": {
    "@types/find-config": "^1.0.0",
    "@types/node": "^14.0.0",
    "@yarnpkg/builder": "^2.1.1",
    "@yarnpkg/core": "^2.1.1",
    "typescript": "^3.9.5"
  }
}
